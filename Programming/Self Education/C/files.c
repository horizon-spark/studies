#include <stdio.h>

int main()
{
    // Создание файла
    // FILE * fopen(имя_файла, режим_открытия);

    // char filename[20];
    // printf("Input file name: ");
    // scanf("%20s",filename);
    // FILE *f = fopen(filename,"w");
    // if (f==NULL)
    // {
    //     perror("Error");
    //     return 1;
    // }
    // fclose(f);
    // printf("Success");
    // return 0;

    // Запись символа в файл  	
    // int putc(int c, FILE *stream);

    // char message[] = "Hello world!";
    // char filename[] = "putc.txt";
    // int len = sizeof(message)/sizeof(message[0]);
    // FILE *f = fopen(filename,"w");
    // for (int i=0;i<len;i++)
    // {
    //     putc(message[i],f);
    // }
    // fclose(f);
    // return 0;

    // Чтение символов из файла
    // int getc(FILE *stream) или int fgetc(FILE *stream)

    // char c;
    // char filename[] = "putc.txt";
    // FILE *f = fopen(filename,"r");
    // if (f)
    // {
    //     while (c=getc(f)!=EOF)
    //     {
    //         printf("%c",c);
    //     }
    // }   
    // fclose(f);
    
}   